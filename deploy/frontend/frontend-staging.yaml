apiVersion: apps/v1beta2
kind: Deployment
metadata:
  name: cip-frontend-deployment
  labels:
    app: cip-frontend
    deployment: {{DEPLOYMENT}}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cip-frontend
  template:
    metadata:
      labels:
        app: cip-frontend
        deployment: {{DEPLOYMENT}}
    spec:
      affinity:
        nodeAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - preference:
              matchExpressions:
              - key: kops.k8s.io/instancegroup
                operator: In
                values:
                - nodes
            weight: 1
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: kops.k8s.io/instancegroup
                operator: In
                values:
                - nodes
                - spot
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app
                  operator: In
                  values:
                  - cip-frontend
              topologyKey: failure-domain.beta.kubernetes.io/zone
            weight: 10
          - podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app
                  operator: In
                  values:
                  - ci-frontend
              topologyKey: kubernetes.io/hostname
            weight: 20
      containers:
      - image: lusotycoon/apache-exporter
        imagePullPolicy: Always
        name: exporter
        ports:
        - containerPort: 9117
          protocol: TCP
        resources:
          requests:
            cpu: 10m
            
      - image: registry.kubernetes.infra.optimy.net/cip/cip-source:{{GO_PIPELINE_LABEL}}
        name: cip-frontend
        imagePullPolicy: Always
        resources:
          requests:
            cpu: 40m
            memory: 128Mi
        volumeMounts:
        - mountPath: /optimy
          name: source
        - mountPath: /etc/apache2/sites-enabled/000-default.conf
          name: cip-frontend-apache-config
          subPath: 000-default.conf

      volumes:
      - name: source
        emptyDir: {}
      - configMap:
          defaultMode: 420
          name: cip-frontend-apache-config
        name: cip-frontend-apache-config
      
       
---
kind: Service
apiVersion: v1
metadata:
  namespace: staging
  name: cip-frontend-service
  labels:
    app: cip-frontend
spec:
  selector:
    app: cip-frontend
  ports:
    - protocol: TCP
      port: 80
      name: cip-frontend-service
    - protocol: TCP
      port: 9117
      name: exporter
  clusterIP: None
---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: cip-frontend-ingress
  namespace: staging
spec:
  rules:
  - host: cip.staging.optimy.net
    http:
      paths:
      - backend:
          serviceName: cip-frontend-service
          servicePort: 80
  - host: '*.front.cip.staging.optimy.net'
    http:
      paths:
      - backend:
          serviceName: cip-frontend-service
          servicePort: 80
          
status:
  loadBalancer: {}
